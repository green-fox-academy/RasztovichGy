#include <stdio.h>
#include <stdlib.h>
#define MAKROVAGYOK 10

enum wtf {LITTLE_WTF, NAGYON_WTF, KOZEPES_WTF};

void nem_tudom_mit_csinal()
{
    int tmp;
}

int main()
{

    int *a,*b;
    a = malloc(sizeof(int));
    b = malloc(sizeof(int));
    *a=10;
    *b=20;

    a = a + b;
    a++;
    if (a < b)
        return -1;

    ~0b110 //ez negálja a bin számot

    for (i)



    printf("%d,%d",*a,*b);



    printf("Hello world!\n");
    return 0;
}
